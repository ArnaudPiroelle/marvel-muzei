<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.arnaudpiroelle.marvelmuzei">

    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Required by RemoteMuzeiArtSource -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
            android:name="com.arnaudpiroelle.marvelmuzei.MarvelMuzeiApplication"
            android:allowBackup="true"
            android:icon="@drawable/ic_launcher"
            android:label="@string/app_name"
            android:theme="@style/Theme.Marvel">

        <activity
                android:name=".ui.launcher.LauncherActivity"
                android:label="@string/title_activity_launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
                android:name=".ui.settings.SettingsActivity"
                android:exported="true"
                android:label="@string/title_activity_settings"/>

        <service
                android:name=".source.MarvelMuzeiArtSource"
                android:description="@string/app_description"
                android:icon="@drawable/ic_launcher"
                android:label="@string/app_name">
            <intent-filter>
                <action android:name="com.google.android.apps.muzei.api.MuzeiArtSource"/>
            </intent-filter>

            <meta-data
                    android:name="settingsActivity"
                    android:value="com.arnaudpiroelle.marvelmuzei.ui.settings.SettingsActivity"/>
            <meta-data
                    android:name="color"
                    android:value="@color/marvel_red"/>
        </service>

        <meta-data android:name="com.crashlytics.ApiKey" android:value="1a5c05cd44da4d5bdb36a429aeb4ac44b61c679e"/>
    </application>

</manifest>
